import React from "react";
import S from "./TextOverColor.Styled";

const TextOverColor = ({ children }) => {
  return <S.TextOverColor>{children}</S.TextOverColor>;
};

export default TextOverColor;
